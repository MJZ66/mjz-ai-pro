# MJZ AI Pro — 项目功能检查报告

| 项目 | 内容 |
|------|------|
| 检查日期 | 2026-06-06 |
| 项目名称 | MJZ AI Pro |
| 检查范围 | 主工程（仓库根目录 / 本地 `代码文件/` 工作副本） |
| 主入口 | `app.py` |
| 检查方式 | 静态代码审查 + `pytest` 全量 + Streamlit 冒烟启动 |
| 检查环境 | Windows · **Python 3.14.0** · pytest 9.x |

---

## 1. 执行摘要

当前工程为 **Python + Streamlit + LLM + RAG + Agent 工具** 分层单体应用：`app.py` 路由三种工作台模式，UI 经 `ui/facade.py` 统一导出，侧栏使用 **原生 `st.sidebar`**。

**自动化测试：`pytest tests/ -q` → 86 passed，0 failed，0 warning**。

**综合结论：** 项目具备可运行、可演示、可维护的基础能力；RAG **Hybrid 检索（BM25 + Vector + RRF）已在代码层实现**，但 **默认仍使用向量检索**；配置有效 API Key 后，对话 / RAG / 附件分析需人工做一次端到端验收。

| 维度 | 评级 | 说明 |
|------|------|------|
| 工程结构 | 良好 | `core` / `agents` / `rag` / `tools` / `ui` / `utils` 分层清晰 |
| 自动化测试 | 良好 | **86/86 通过，0 warning**（19 个测试文件） |
| 服务启动 | 通过 | `streamlit run app.py` 可监听 8501 |
| 安全 | 通过 | 无硬编码 `sk-` Key；`.env` / `data/metrics.json` 已忽略 |
| 端到端 AI 调用 | 待人工验证 | 自动化未调用远端 LLM API |
| 文档 | 已同步 | README 与本文档已更新至 2026-06-06 状态 |

---

## 2. 状态分类说明

| 类别 | 含义 | 示例 |
|------|------|------|
| **已实现** | 代码已落地且测试或静态审查通过 | 三模式路由、RAG 入库、AST 计算器、对话导出 Markdown |
| **已修复** | 近期缺陷已关闭 | Python 3.14 下 `calculator` 兼容 `ast.Num` 移除（A1）；PDF 依赖 PyPDF2→pypdf（D1） |
| **代码层已完成，默认未启用** | 功能已实现并有测试，运行时未默认打开 | **Hybrid Retrieval**（BM25 + Vector + RRF；`.env` 设 `RAG_RETRIEVAL_MODE=hybrid`） |
| **后续规划** | README「未来规划」或未排期项 | Docker、Hybrid UI 开关、更多 Agent |

---

## 3. 项目结构检查

### 3.1 当前目录结构（核心）

```
mjz-ai-pro/
├── app.py                      # 主入口（三模式，~540 行）
├── common.py                   # build_llm_client
├── config.py                   # 兼容旧导入（主配置见 core/config.py）
├── requirements.txt
├── .env.example
├── .gitignore
├── .streamlit/config.toml
├── agents/                     # ChatAgent、BaseAgent、prompts（6 角色）
├── core/                       # config、llm_client
├── rag/                        # 文档加载、分块、ChromaDB、BM25、RRF、检索、RAGAgent
│   ├── bm25_index.py           # BM25 索引（bm25_index.json）
│   ├── hybrid_fusion.py        # RRF 融合
│   └── retriever.py            # mode=vector（默认）/ hybrid
├── tools/                      # calculator（AST）、text_summary
├── ui/
│   ├── facade.py               # app.py 唯一 UI 导入门面（含导入兜底）
│   ├── layout.py               # 侧栏、聊天区、输入栏、空状态
│   ├── theme.css / theme.py    # 工作台样式（原生 sidebar 美化）
│   ├── upload_zone.py          # 拖拽上传
│   ├── panels.py、components.py、animations.py
│   └── gsap_runtime.js
├── utils/                      # file_utils、attachment_context、session、
│                               # stream_ui、metrics、math_intent、logger
├── data/
│   └── metrics.seed.json       # 运行统计示例基线（可提交；运行时 metrics.json 已忽略）
├── tests/                      # 19 个测试文件，86 条用例
├── docs/
├── assets/                     # README 运行截图（7 张）
└── SECURITY.md
```

### 3.2 主流程（已实现）

1. `with st.sidebar: render_sidebar()` — 工作模式、模型、智能体、设置、运行统计。
2. 主区按模式渲染：`多轮对话` / `RAG 知识库` / `Agent 工具`。
3. 多轮对话：附件上传暂在 **sidebar expander**；底部 `st.chat_input` 输入。
4. UI 层：**禁止** app.py 直接 import `ui/layout.py`，统一走 `ui.facade`。

---

## 4. 自动化检查结果

### 4.1 单元测试汇总

```text
pytest tests/ -q
86 passed in ~3s
```

| 测试文件 | 用例数 | 主要覆盖 |
|----------|--------|----------|
| test_config.py | 9 | 厂商 Key、Base URL、缺 Key 提示 |
| test_file_utils.py | 7 | 文本截断、TXT 读取 |
| test_attachment_context.py | 7 | 附件注入 System、状态行 |
| test_calculator.py | 5 | AST 安全求值、除零、非法表达式 |
| test_math_intent.py | 3 | 自然语言算式、非数学返回 None |
| test_session_utils.py | 4 | 智能体切换、reset_system |
| test_document_loader.py | 4 | RAG 加载、图片拒绝 |
| test_metrics.py | 3 | 访问/LLM/入库指标持久化 |
| test_embedding_config.py | 2 | DeepSeek 对话 + 通义 Embedding |
| test_prompts.py | 2 | 角色 Prompt、别名 |
| test_text_splitter.py | 2 | 分块 |
| test_core_config.py | 1 | core 配置 |
| test_retriever.py | 4 | 向量检索、mode 分派 |
| test_hybrid_fusion.py | 8 | RRF 融合 |
| test_bm25_index.py | 4 | BM25 分词、检索、持久化 |
| test_hybrid_retriever.py | 5 | Hybrid 集成、fallback |
| test_rag_agent.py | 3 | ingest 同步 BM25 |
| test_export_chat.py | 6 | 对话导出 Markdown |
| test_rag_retrieval_config.py | 7 | `RAG_RETRIEVAL_MODE` 解析与 RAGAgent 联动 |
| **合计** | **86** | |

### 4.2 已修复项（A1、D1）

| 任务 | 问题 | 修复 |
|------|------|------|
| **A1** | Python 3.14 下计算器 6 条测试失败（`ast.Num` 已移除） | `tools/calculator.py` 使用 `getattr(ast, "Num", None)` 守卫旧分支 |
| **D1** | PyPDF2 DeprecationWarning；依赖已废弃 | `utils/file_utils.py` 改用 `pypdf.PdfReader`；`requirements.txt` 移除 PyPDF2、增加 `pypdf` |

验证：A1 — `test_calculator` + `test_math_intent` 共 8 条通过；D1 — 全量 `pytest -q` **86 passed，0 warning**。

### 4.2.1 Hybrid Retrieval（代码层已完成，默认未启用）

| 项 | 说明 |
|----|------|
| 模块 | `rag/bm25_index.py`、`rag/hybrid_fusion.py`、`rag/retriever.py` |
| 入库 | `RAGAgent.ingest_upload` 同步写入 `{persist_dir}/bm25_index.json` |
| 融合 | 向量 Top-K + BM25 Top-K → **RRF**（`rrf_fuse`） |
| 默认行为 | **`RAG_RETRIEVAL_MODE=vector`**（或未设置）→ 纯向量检索 |
| 启用方式 | `.env` 设置 **`RAG_RETRIEVAL_MODE=hybrid`**；`core/config.resolve_rag_retrieval_mode()` |
| 非法值 | 静默回退为 `vector` |
| UI | **未暴露**开关；`app.py` 未改 |
| 测试 | `test_hybrid_fusion`、`test_bm25_index`、`test_hybrid_retriever`、`test_rag_agent`、`test_rag_retrieval_config` |

### 4.3 已知问题（非阻塞）

| 项 | 状态 | 说明 |
|----|------|------|
| 附件在 sidebar 而非底部输入框 | **已实现（临时方案）** | 为布局稳定暂放 expander；回到底部为后续任务 |
| 侧栏 `components.html` 自动展开 | **已实现（兼容 hack）** | 历史折叠状态恢复；稳定后可评估删除 |
| PDF 二进制单元测试 | **后续** | 无小型 PDF fixture |
| CI / Docker | **后续 B3 / 规划** | 未配置 GitHub Actions |

### 4.4 安全扫描

| 项 | 结果 |
|----|------|
| 硬编码 API Key | 未发现 |
| `.env` / `data/metrics.json` / 向量库 | `.gitignore` 已排除 |
| 计算器 | AST 白名单，无 `eval` |
| RAG 图片 | `document_loader` 拒绝图片入库 |

### 4.5 Streamlit 冒烟

| 项 | 结果 |
|----|------|
| 命令 | `streamlit run app.py` |
| 地址 | http://localhost:8501 |
| 侧栏 | 原生 `st.sidebar`，勿 CSS 隐藏 toolbar |

---

## 5. 功能逐项检查

### 5.1 三模式（已实现）

| 模式 | 核心模块 | 检查结论 |
|------|----------|----------|
| 多轮对话 | `ChatAgent` + `attachment_context` + `math_intent` | 通过（单元测试 + 静态审查） |
| RAG 知识库 | `RAGAgent` + ChromaDB + BM25 索引 + `Retriever` + 引用卡片 | 通过；**默认向量检索**；Hybrid 有集成测试 |
| Agent 工具 | `BaseAgent` + `calculator` + `text_summary` | 通过（计算器 3.14 已修复） |

### 5.2 多智能体（已实现）

| 智能体（UI 显示） | System Prompt |
|-------------------|---------------|
| 通用聊天助手 | 已定义 |
| 法律助手 | 已定义 |
| 代码助手 | 已定义 |
| 简历分析助手 | 已定义 |
| 文件助手 | 已定义（别名：PDF总结助手） |
| 小红书爆款文案助手 | 已定义（别名：小红书文案助手） |

### 5.3 配置与模型（已实现）

- 通义 / DeepSeek 切换：`core/config.py`
- **RAG 检索模式**：`RAG_RETRIEVAL_MODE=vector`（默认）| `hybrid`（BM25 + Vector + RRF）
- 侧边栏 Key 覆盖、缺 Key 友好停止：`app.py` + 测试覆盖
- RAG Embedding：DeepSeek 对话时走通义 DashScope（`test_embedding_config`）

### 5.4 UI（已实现）

| 项 | 说明 |
|----|------|
| 布局 | 原生 sidebar + 主区聊天工作台 |
| 主题 | `ui/theme.css`（气泡、侧栏间距、输入框、空状态已小步优化） |
| 导入 | `ui/facade.py` 注册表 + 缺失符号兜底 |

**待人工验证：** 真实 Key 下流式对话、RAG 问答、附件分析效果。

---

## 6. 环境与 Git

### 6.1 推荐启动

```bash
python -m venv .venv
# Windows: .venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env   # 填入 Key，勿提交
pytest tests/ -q         # 期望 86 passed，0 warning
streamlit run app.py
```

### 6.2 Python 版本

| 范围 | 说明 |
|------|------|
| 推荐 | 3.10 – 3.13 |
| 已验证 | **3.14.0**（开发与全量测试环境） |

### 6.3 Git 远程

- 仓库：https://github.com/MJZ66/mjz-ai-pro.git
- 默认分支：`main`

---

## 7. 人工验收清单

在配置有效 API Key 后建议逐项勾选：

- [ ] 页面显示侧栏与三模式切换
- [ ] 未配置 Key 时出现警告并停止主流程
- [ ] 多轮对话流式回复正常
- [ ] 输入 `(125+76)*9` 或「计算 (125+76)*9」可得到本地算式结果
- [ ] sidebar「附件上传」解析后可连续追问
- [ ] RAG 上传文档 → 构建索引 → 提问 → 显示引用
- [ ] Agent 工具：计算器 / 文本摘要可用
- [ ] 侧栏折叠后可再通过原生按钮展开

---

## 8. 后续路线（与 README 对齐）

| 阶段 | 任务 | 状态 |
|------|------|------|
| A | calculator Python 3.14 兼容 | **已完成** |
| B | 文档同步（README + 本报告） | **本次完成** |
| B3 | GitHub Actions CI | **已完成** |
| C | `theme.css` 建议按钮 / 引用卡片 / 上传区 | 部分已完成 |
| D1 | PyPDF2 → pypdf | **已完成** |
| D2 | 对话导出 Markdown | **已完成** |
| Hybrid | BM25 + Vector + RRF；`RAG_RETRIEVAL_MODE=hybrid` | **代码层已完成，默认 vector** |
| 后续 | Hybrid UI 开关、Docker、更多 Agent | 规划中 |

---

## 9. 结论

**MJZ AI Pro** 当前状态：

1. **结构完整**：Core / Agents / RAG / Tools / UI / Utils 分层，facade 稳定导入。
2. **测试绿线**：86/86 自动化测试通过，0 warning（Python 3.14.0）。
3. **功能闭环**：三模式、附件、RAG（默认向量 + Hybrid 代码就绪）、Agent 工具均已实现；LLM 端到端需人工 Key 验证。
4. **安全基线**：无硬编码密钥，计算器 AST 白名单，敏感路径已 ignore。

适合作为简历项目、课程演示与面试讲解；提交前建议完成第 7 节人工清单。

---

*报告更新日期：2026-06-06 · 检查环境：Windows · Python 3.14.0 · pytest 86 passed，0 warning · Hybrid 可通过 RAG_RETRIEVAL_MODE 启用（默认 vector）*
